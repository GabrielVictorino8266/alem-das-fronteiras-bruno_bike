# Além das Fronteiras — API

API Django do projeto Além das Fronteiras.

## Requisitos

- Docker
- Docker Compose

## Como rodar

```bash
docker-compose up --build
```

A API estará disponível em `http://localhost:8000`.

## Parar

```bash
docker-compose down
```