info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "gabriel.victorino@email.com",
        "password": "gabriel.victorino"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        console.log(data);

        if(data.access){
          bru.setEnvVar("access", data.access);
        }

        if(data.refresh){
          bru.setEnvVar("refresh", data.refresh);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
