info:
  name: Criar Usuário
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/users"
  body:
    type: json
    data: |-
      {
        "email": "gabriel.victorino@email.com",
        "password": "gabriel.victorino"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
